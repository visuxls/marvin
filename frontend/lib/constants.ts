/** sessionStorage key for the active conversation thread. */
export const ACTIVE_CONVERSATION_KEY = "marvin:active-conversation";

/** localStorage key for desktop sidebar collapsed state. */
export const SIDEBAR_COLLAPSED_KEY = "marvin:sidebar-collapsed";

/** localStorage key for the selected reasoning effort. */
export const REASONING_EFFORT_KEY = "marvin:reasoning-effort";

/** Server snapshot placeholder for useSyncExternalStore hydration. */
export const SERVER_CONVERSATION_PLACEHOLDER = "default";

/** Max width class shared by chat column sections. */
export const CHAT_COLUMN = "mx-auto w-full max-w-3xl px-4";
